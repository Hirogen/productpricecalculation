<UserControl x:Class="ProductPriceCalculator.Views.CategoriesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <StackPanel>
        <!-- Header -->
        <TextBlock Text="{Binding HeaderCategories}"
                   FontSize="20"
                   FontWeight="Bold"
                   Margin="0,0,0,10"/>
        <Rectangle Height="2" Fill="#0078D7" Margin="0,0,0,15"/>

        <!-- Info Banner -->
        <Border Background="#E6F2FF"
                BorderBrush="#0078D7"
                BorderThickness="2"
                CornerRadius="5"
                Padding="10"
                Margin="0,0,0,15">
            <TextBlock Text="{Binding InfoCategories}"
                       TextWrapping="Wrap"
                       FontWeight="SemiBold"/>
        </Border>

        <!-- Add New Category Section -->
        <Border Background="WhiteSmoke"
                BorderBrush="LightGray"
                BorderThickness="1"
                CornerRadius="5"
                Padding="15"
                Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="{Binding HeaderAddNewCategory}"
                           FontSize="14"
                           FontWeight="Bold"
                           Margin="0,0,0,10"/>

                <Label Content="{Binding LabelCategoryName}" FontWeight="SemiBold"/>
                <TextBox Text="{Binding NewCategoryName, UpdateSourceTrigger=PropertyChanged}"
                         Height="25"
                         Margin="0,0,0,10"/>

                <Label Content="{Binding LabelCategoryDescription}" FontWeight="SemiBold"/>
                <TextBox Text="{Binding NewCategoryDescription, UpdateSourceTrigger=PropertyChanged}"
                         Height="60"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         Margin="0,0,0,10"/>

                <Button Content="{Binding ButtonAddCategory}"
                        Command="{Binding AddCategoryCommand}"
                        Style="{StaticResource SuccessButtonStyle}"
                        Width="150"
                        HorizontalAlignment="Left"/>
            </StackPanel>
        </Border>

        <!-- Current Categories List -->
        <TextBlock Text="{Binding HeaderCurrentCategories}"
                   FontSize="16"
                   FontWeight="Bold"
                   Margin="0,0,0,10"/>

        <DataGrid ItemsSource="{Binding Categories}"
                  SelectedItem="{Binding SelectedCategory}"
                  AutoGenerateColumns="False"
                  Height="250"
                  Margin="0,0,0,10"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="{Binding DataContext.ColCategoryName, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                    Binding="{Binding Name}" 
                                    Width="200"/>
                <DataGridTextColumn Header="{Binding DataContext.ColCategoryDescription, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                    Binding="{Binding Description}" 
                                    Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Delete Button -->
        <Button Content="{Binding ButtonDeleteCategory}"
                Command="{Binding DeleteCategoryCommand}"
                Style="{StaticResource DangerButtonStyle}"
                Width="200"
                HorizontalAlignment="Left"/>
    </StackPanel>
</UserControl>
