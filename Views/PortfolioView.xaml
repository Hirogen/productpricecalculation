<UserControl x:Class="ProductPriceCalculator.Views.PortfolioView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <StackPanel>
        <!-- Header -->
        <TextBlock Text="{Binding HeaderProductPortfolio}"
                   FontSize="20"
                   FontWeight="Bold"
                   Margin="0,0,0,10"/>
        <Rectangle Height="2" Fill="#0078D7" Margin="0,0,0,15"/>

        <!-- Info Banner -->
        <Border Background="#E6F2FF"
                BorderBrush="#0078D7"
                BorderThickness="2"
                CornerRadius="5"
                Padding="10"
                Margin="0,0,0,15">
            <TextBlock Text="{Binding InfoPortfolio}"
                       TextWrapping="Wrap"
                       FontWeight="SemiBold"/>
        </Border>

        <!-- Product Selection -->
        <TextBlock Text="{Binding HeaderProductSelection}"
                   FontSize="14"
                   FontWeight="Bold"
                   Margin="0,0,0,10"/>

        <TextBlock Text="{Binding InfoSelectProducts}"
                   FontSize="12"
                   TextWrapping="Wrap"
                   Foreground="Gray"
                   Margin="0,0,0,10"/>

        <DataGrid ItemsSource="{Binding AvailableProducts}"
                  AutoGenerateColumns="False"
                  Height="200"
                  Margin="0,0,0,15">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="{Binding DataContext.ColSelected, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                        Binding="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" 
                                        Width="80"/>
                <DataGridTextColumn Header="{Binding DataContext.ColProduct, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                    Binding="{Binding Product.Name}" 
                                    Width="*" 
                                    IsReadOnly="True"/>
                <DataGridTextColumn Header="{Binding DataContext.ColBaseCost, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                    Binding="{Binding Product.BaseCost, StringFormat=C2}" 
                                    Width="100" 
                                    IsReadOnly="True"/>
                <DataGridTextColumn Header="{Binding DataContext.ColMarkup, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                    Binding="{Binding Product.Markup, StringFormat=F0}" 
                                    Width="80" 
                                    IsReadOnly="True"/>
                <DataGridTextColumn Header="{Binding DataContext.ColExpectedUnits, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                    Binding="{Binding Product.ExpectedMonthlyUnits, StringFormat=F0}" 
                                    Width="130" 
                                    IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>

        <Button Content="{Binding ButtonCalculatePortfolio}"
                Command="{Binding CalculatePortfolioCommand}"
                Style="{StaticResource PrimaryButtonStyle}"
                Width="250"
                Margin="0,0,0,20"
                HorizontalAlignment="Left"/>

        <!-- Results Section -->
        <StackPanel Visibility="{Binding HasResults, Converter={StaticResource BoolToVisibilityConverter}}">
            <TextBlock Text="{Binding HeaderPortfolioAnalysis}"
                       FontSize="16"
                       FontWeight="Bold"
                       Margin="0,10,0,10"/>

            <!-- Summary Info -->
            <Border Background="#FFFACD"
                    BorderBrush="#FFD700"
                    BorderThickness="2"
                    CornerRadius="5"
                    Padding="10"
                    Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock FontSize="13" FontWeight="SemiBold">
                        <Run Text="{Binding LabelTotalPortfolioUnits, Mode=OneWay}"/>
                        <Run Text=" "/>
                        <Run Text="{Binding PortfolioResult.TotalUnits, StringFormat=F0}"/>
                    </TextBlock>
                    <TextBlock FontSize="13" FontWeight="SemiBold">
                        <Run Text="{Binding LabelDistributedOperatingCost, Mode=OneWay}"/>
                        <Run Text=" "/>
                        <Run Text="{Binding PortfolioResult.TotalOperatingCosts, StringFormat=C2}"/>
                        <Run Text="/month"/>
                    </TextBlock>
                </StackPanel>
            </Border>

            <!-- Results Grid -->
            <DataGrid ItemsSource="{Binding PortfolioResult.ProductResults}"
                      AutoGenerateColumns="False"
                      Height="250"
                      Margin="0,0,0,15"
                      IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="{Binding DataContext.ColProductName, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                        Binding="{Binding ProductName}" 
                                        Width="*"/>
                    <DataGridTextColumn Header="{Binding DataContext.ColExpectedUnits, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                        Binding="{Binding ExpectedUnits, StringFormat=F0}" 
                                        Width="120"/>
                    <DataGridTextColumn Header="{Binding DataContext.ColPercentage, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                        Binding="{Binding Percentage, StringFormat=F1}" 
                                        Width="90"/>
                    <DataGridTextColumn Header="{Binding DataContext.ColOperatingCostPerUnit, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                        Binding="{Binding OperatingCostPerUnit, StringFormat=C2}" 
                                        Width="120"/>
                    <DataGridTextColumn Header="{Binding DataContext.ColFinalPrice, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                        Binding="{Binding FinalPrice, StringFormat=C2}" 
                                        Width="100"/>
                    <DataGridTextColumn Header="{Binding DataContext.ColProfit, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                        Binding="{Binding ProfitPerUnit, StringFormat=C2}" 
                                        Width="100"/>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Explanation -->
            <Border Background="#E6F2FF"
                    BorderBrush="#0078D7"
                    BorderThickness="1"
                    CornerRadius="5"
                    Padding="10">
                <TextBlock Text="{Binding InfoPortfolio}"
                           TextWrapping="Wrap" 
                           FontSize="12"/>
            </Border>
        </StackPanel>
    </StackPanel>
</UserControl>
